# associate_images_with_article()

## Positive cases

-   [x] given valid HTML with image URLs → calls `extract_image_paths()`
-   [x] given valid HTML with image URLs → success to associate `ArticleImage` with article id
